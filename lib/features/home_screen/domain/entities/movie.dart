class Movie {
  int id;
  String posterPath;
  String movieTitle;
  String mediaType;

  Movie(
      {required this.movieTitle,
      required this.posterPath,
      required this.id,
      required this.mediaType});
}
